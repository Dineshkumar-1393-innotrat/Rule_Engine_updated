// Add this helper function BEFORE the PALETTE_GROUPS definition (around line 1615):

// Helper function to convert shapeData to palette items
const createPaletteItemsFromShapeData = (categoryName) => {
  const shapes = shapeData[categoryName] || [];
  return shapes.map(shape => ({
    type: 'genericShape',
    label: shape.name,
    shapeId: shape.id,
    icon: (
      <svg viewBox={shape.icon.viewBox} preserveAspectRatio="xMidYMid meet" style={{ width: '100%', height: '100%' }}>
        <path 
          d={shape.icon.path} 
          fill={shape.icon.fill || '#fff'} 
          stroke={shape.icon.stroke || '#111'} 
          strokeWidth={shape.icon.strokeWidth || 2}
        />
      </svg>
    ),
  }));
};

// Add these new categories to PALETTE_GROUPS array (after line 1616, before the 'blocks' category):

  {
    id: 'flowchart',
    title: 'Flowchart',
    items: createPaletteItemsFromShapeData('Flowchart'),
  },

// Add these new categories AFTER the 'blocks' category and BEFORE the 'connectors' category:

  {
    id: 'basic',
    title: 'Basic Shapes',
    items: createPaletteItemsFromShapeData('Basic'),
  },
